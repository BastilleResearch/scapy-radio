# auto-generated by grc.converter

id: scapy_radio_strip_gr_header
label: Strip GR header
category: '[Scapy Radio]'

parameters:
-   id: protocol_id
    label: Protocol
    dtype: int

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
asserts:
- ${ protocol_id > 0 and protocol_id < 256 }

templates:
    imports: import gnuradio.scapy_radio as scapy_radio
    make: scapy_radio.strip_gr_header(${protocol_id})
    callbacks:
    - set_protocol_id(${protocol_id})

file_format: 1
