# auto-generated by grc.converter

id: zigbee_packet_sink_scapy
label: packet_sink_scapy
category: '[zigbee]'

parameters:
-   id: threshold
    label: threshold
    dtype: int

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: message
    id: out

templates:
    imports: import gnuradio.zigbee as zigbee
    make: zigbee.packet_sink_scapy(${threshold})

file_format: 1
